version: "2"
services:
  node:
    image: "node:17"
    user: "node"
    working_dir: /home/node/app
    environment:
      - NODE_ENV=production
    volumes:
      - ./build/:/home/node/app
    expose:
      - "3000"
    command: "npm start"